# context/ — 세션 간 인수인계 노트

`landing_mpc` 작업 중 **코드나 git 로그만 봐서는 알 수 없는 것**들을 모아둔 곳.
설계 근거, SITL에서만 드러난 함정, 그리고 **시도했다가 실패해서 되돌린 것들**.

| 파일 | 내용 |
|---|---|
| `project_landing_mpc.md` | 파이프라인 구조, 원뿔 기하, SITL 함정, 검증 수치, 기각된 가설 |
| `feedback_gentle_control.md` | "P 게인 낮게, 급가속 금지" — 실기 안전 원칙과 그 이유 |

## 왜 따로 두나

- `docs/nodes.md` 는 **무엇을 어떻게** (수식·구조) — 지금 코드의 설명
- `docs/context/` 는 **왜 이렇게, 무엇을 하지 말 것** — 되돌린 시도와 그 근거

특히 **기각된 가설**이 중요하다. 이게 없으면 "표적 속도를 낮춰보자", "a_max를
줄여보자" 같은 이미 반증된 시도를 반복하게 된다. 각 항목에 측정값이 붙어 있으니
다시 하기 전에 먼저 읽을 것.

## Claude Code CLI 로 이어서 작업할 때

이 리포 경로(`/home/seyeongw/ros2_ws/PX4-ROS2`)에서 CLI를 실행하면 동일한
프로젝트 메모리가 자동으로 로드되므로, 보통은 이 폴더를 따로 읽힐 필요가 없다.
여기 사본은 **버전관리·팀 공유·`~/.claude` 유실 대비**용이다.

원본 위치: `~/.claude/projects/-home-seyeongw-ros2-ws-PX4-ROS2/memory/`
