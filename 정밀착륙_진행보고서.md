# ArUco 마커 정밀착륙 시스템 — 진행 보고서

**PX4-ROS2 / ArduPilot SITL · Gazebo Harmonic · MAVROS**

작성일: 2026-06-02 · 브랜치: wang

---

## 1. 개요

하방 카메라로 지면의 ArUco 마커(DICT_4X4_50, ID 0)를 검출하여 그 위로 자동 정밀착륙하는 시스템을 구현하였다.  
드론은 스스로 **GUIDED 전환 → 시동 → 이륙 → 마커 정렬 → 하강 → 착륙**까지 자율 수행한다.

**파이프라인:**
```
Gazebo(물리/카메라) → ArduPilot SITL → MAVROS(ROS 2 브리지) → ArUco 검출 노드 → 정밀착륙 제어 노드
```

---

## 2. 작업 환경

| 항목 | 버전 |
|------|------|
| ROS 2 | Humble (Ubuntu 22.04, Python 3.10) |
| Gazebo | Harmonic (gz-sim) + ardupilot_gazebo 플러그인 |
| ArduPilot | SITL (ArduCopter, JSON 모델로 Gazebo 연동) |
| MAVROS | apm.launch, UDP 연결 |
| OpenCV | 4.13 |
| NumPy | 2.2.6 |

---

## 3. 주요 산출물

### 3.1 신규 Python 패키지: `precision_landing`

정밀착륙 제어기를 독립 패키지로 분리(ament_python).

- `precision_landing/precision_landing_node.py` — 상태기계 + 칼만필터 + 속도제어
- `precision_landing/launch/precision_landing.launch.py` — 풀 bringup (MAVROS + 카메라 브리지 + 검출 + 제어 일괄 실행)

### 3.2 변경/추가된 기존 자산

| 파일 | 변경 내용 |
|------|-----------|
| `camera_detection/aruco_detector_node.py` | ArUco 검출 (cv_bridge 제거) |
| `offboard/src/drone_core_controller.cpp` | 참고 코드(서비스 기반 이착륙) C++ 포팅 |
| `gazebo/models/aruco_marker_0/model.sdf` | 마커 머티리얼 수정 |
| `gazebo/worlds/iris_down_camera_runway.sdf` | 마커 위치 조정 |
| `camera_detection/launch/precland_bringup.launch.py` | Python 제어기로 전환 |

---

## 4. 진행 내역 및 해결한 문제

### 4.1 제어기 구조 정립

- 사용자 제공 참고 코드(MAVROS GUIDED, CommandTOL 이착륙)를 C++로 포팅하여 `drone_core_controller` 작성.
- 정밀착륙 제어기를 C++(offboard)에서 독립 Python 패키지로 이관.
- 검출+제어+MAVROS+브리지를 런치 하나로 통합, 3터미널 실행 흐름 확립.

### 4.2 MAVROS 연결 실패 ("UDP 닫힘")

- **증상:** MAVROS 연결 시 "connection refused / UDP closed"
- **원인:** `fcu_url`이 `udp://127.0.0.1:14550@14555` — 14555(빈 포트)로 송신하여 ICMP port-unreachable 발생. MAVProxy는 14550으로만 송신.
- **해결:** `fcu_url`을 `udp://:14550@` 로 변경 (14550 바인드 + remote 자동 학습)

### 4.3 시동은 되나 이륙하지 않음

- **증상:** armed 되지만 지상에 머무름
- **원인:** ArduCopter GUIDED는 올라가는 position setpoint 스트리밍만으로 이륙하지 않음 — 명시적 takeoff 명령 필요
- **해결:** TAKEOFF 단계에서 `/mavros/cmd/takeoff`(CommandTOL) 서비스 호출로 변경

### 4.4 ArUco 마커가 검은색으로 표시

- **증상:** 마커가 패턴 없이 검은 사각형
- **원인:** PBR `<metal>` 워크플로의 metalness 기본값 1.0 → 반사맵 없는 완전 금속 표면이 검게 렌더 (텍스처 PNG 자체는 정상)
- **해결:** `metalness=0`, `roughness=1`, `ambient`/`diffuse` 추가

### 4.5 마커가 카메라 시야 밖

- **원인:** 마커(x=3)와 드론 스폰(x=0) 거리 과다
- **해결:** 화각(1.20 rad, 5m 고도에서 ~3.4m) 고려해 마커를 x=1로 이동

### 4.6 정렬·하강 자체가 동작하지 않음 (핵심 결함)

- **증상:** 검출 디버그 토픽(`/perception/aruco_debug`)이 아예 안 뜨고 드론이 안 움직임
- **원인:** NumPy 2.2.6 환경에서 ROS `cv_bridge`(numpy 1.x 컴파일 C 확장)가 런타임 변환(`imgmsg_to_cv2`)에서 `_ARRAY_API` 오류로 붕괴 → 검출 콜백이 매 프레임 사망 → perception 토픽 미발행 → 제어기가 IDLE에 고착
- **해결:** 검출 노드에서 `cv_bridge` 제거, `np.frombuffer + cv2.imencode`로 직접 변환 (cv2 4.13은 numpy 2와 정상 동작)

### 4.7 정렬 방향·회전 문제

- **원인:** `setpoint orientation.w=1` (yaw=0) 명령으로 yaw 90° 스폰 기체가 회전 → 카메라 이미지축 ↔ 월드축 매핑 붕괴
- **해결:**
  1. yaw 고정 (현재 heading 유지)
  2. 이미지 offset을 바디 오차로 보고 yaw로 월드 ENU 회전변환
  3. 부호 튜닝 파라미터(`lat_swap`/`lat_sign_*`) 실시간 조정

### 4.8 정렬 게인 과대 → 좌우 진동

- **1차 개선:** 고도 반영 변환(픽셀 → 실제 m) + 게인을 "남은 거리의 비율"로 → 하강 시 과보정 방지
- **2차 개선:** 위치(position) 명령은 이중 위치 루프+지연으로 본질적 진동 → **속도(velocity) 제어로 전환**. `v = vel_gain × 오차` (1차 지수응답, 오버슈트 없음)

### 4.9 수치해석 기법 도입 — 칼만 필터

- **등속(constant-velocity) 칼만 필터**로 마커 월드 위치(E, N) + 속도 추정
- **효과:** 검출 노이즈 평활(검증상 0.15m → 0.063m, 약 2.4배), 마커 놓친 프레임은 예측(coast)으로 진행(1.5s까지) → 끊김에 강건
- **구조:** Predict(매 틱) – Update(검출 시), 칼만 게인으로 예측·측정 자동 가중

### 4.10 착륙 직전 정렬 게이트 (안전장치)

`land_alt`(0.7m) 도달 시 무조건 착륙하지 않고, KF 수평오차가 `land_align_radius`(0.20m) 이내일 때만 LAND.  
빗나가면 재정렬 → 오착륙 방지.

---

## 5. 현재 제어 알고리즘 요약

**상태기계:** `TAKEOFF → IDLE → ALIGN → DESCEND → DONE(LAND)`

| 상태 | 동작 |
|------|------|
| `TAKEOFF` | GUIDED 전환 → 시동 → CommandTOL 이륙 → `flight_alt` 도달 시 IDLE |
| `IDLE` | 제자리 유지(속도 0), 마커 감지되면 ALIGN |
| `ALIGN` | 칼만 추정 위치로 속도 제어, 수평오차 < `align_radius`면 DESCEND |
| `DESCEND` | 정렬 유지하며 하강(짧은 끊김은 coast), `land_alt`에서 정렬 게이트 통과 시 DONE |
| `DONE` | ArduCopter LAND 모드로 최종 착륙 |

---

## 6. 남은 작업 및 검증 필요 사항

- [ ] 횡방향 부호(`lat_sign_*`) 비행 검증 — 카메라 장착 기준 정/반 확정
- [ ] 속도 게인(`vel_gain`)·칼만 파라미터(`kf_meas_std`/`kf_accel_std`) 실비행 튜닝 후 launch 기본값에 고정
- [ ] 착륙 게이트 저고도 핑퐁 여부 확인 후 `land_align_radius` 확정
- [ ] 레거시 C++ 제어기(`offboard/src/precision_landing.cpp`) 정리 또는 동일 수정 반영
- [ ] `yolo_processor` 노드도 동일 NumPy 2.x 이슈 존재 — 필요 시 cv_bridge 제거 적용

---

## 7. 실행 방법 (3 터미널)

**사전 준비:**
```bash
python3 gazebo/gen_aruco_model.py   # 텍스처 1회 생성
colcon build --symlink-install --packages-select camera_detection precision_landing
```

**터미널 1 — Gazebo:**
```bash
cd ~/ros2_ws/PX4-ROS2/gazebo && ./run_sim.sh
```

**터미널 2 — ArduPilot SITL:**
```bash
cd ~/ardupilot && sim_vehicle.py -v ArduCopter -f gazebo-iris --model JSON --console --map
```

**터미널 3 — ROS 스택 일괄:**
```bash
source install/setup.bash
source ~/ros_gz_ws/install/setup.bash
ros2 launch precision_landing precision_landing.launch.py
```

**모니터링:**
```bash
ros2 topic echo /precision_landing/debug
ros2 run rqt_image_view rqt_image_view /perception/aruco_debug/compressed
```

---

## 8. 추가 진행 내역 (2026-06-03 갱신)

2026-06-02 이후 SITL 실비행 검증을 수행하여 6장에서 남겨 두었던 항목 대부분을 해소하였다.
**최종 착륙 오차 약 2 cm**로 풀 정밀착륙 시퀀스를 SITL에서 확인하였다.

### 8.1 횡방향 부호 매핑 확정 (6.1 항목 해결)

- **검증 결과:** `lat_swap=false`, `lat_sign_fwd=+1`, `lat_sign_left=+1` 조합이 정상.
- **분석:** 이전에 관찰된 "맴도는(circling)" 증상은 부호/회전 오류가 아니라 **게인 과대로 인한 리밋 사이클(한계진동)**이었음 — 이미지→바디→ENU 변환 매핑 자체는 올바름.
- `_track`에 ~1 Hz 스로틀 진단 로그(off/yaw/err/cmd) 추가로 매핑을 실시간 확인 가능.

### 8.2 속도 게인 튜닝 — 좌우 리밋 사이클 제거 (6.2 항목 해결)

- 속도 서보 기본값 하향: `vel_gain` 0.8 → **0.4**, `vel_max` 1.0 → **0.5**.
- 효과: 좌우 한계진동이 사라지고 **하강 전 구간에서 양 축이 깔끔하게 수렴**.
- `lat_swap` / `lat_sign_fwd` / `lat_sign_left` / `vel_gain` / `vel_max`를 launch 인자(ParameterValue 타입 지정)로 노출 → 실시간 튜닝 가능.

### 8.3 정렬 비례 하강 (descent scaling)

- DESCEND 단계의 하강률에 `descent_scale = 1 − (수평오차 / align_radius)` 적용.
- 중심에서 벗어나면 하강을 늦추거나 멈추고, 재정렬되면 다시 하강 → 빗나간 채 내려가는 현상 방지.

### 8.4 검출 끊김에 강건한 하강 게이트

- 하강 조건을 `marker_ok`(현재 ArUco 패턴 가시 여부)가 아니라 `kf_init`(마커 중심 월드 위치 확보 여부)로 변경.
- 순간적인 검출 누락이 있어도 칼만 추정 위치로 계속 접근.

### 8.5 칼만 가속도 잡음 하향

- `kf_accel_std` 기본값 0.5 → **0.1**: 정지 마커는 속도 상태가 0에 가까워야 하므로 더 평활하게 추정.
- (이동 마커 추적이 필요할 때는 값을 키운다.)

### 8.6 임무 종료 후 자동 종료

- DONE 단계에서 `/mavros/state`로 **착지 + 자동 시동해제(disarm)를 확인**한 뒤 `rclpy.shutdown()` 호출 → 노드가 임무 후 스스로 종료.
- `main()`은 `rclpy.ok()`로 이중 shutdown을 방어.

### 8.7 갱신된 남은 작업

- [x] 횡방향 부호(`lat_sign_*`) 검증 — `swap=false/fwd=+1/left=+1` 확정 (8.1)
- [x] 속도 게인·칼만 파라미터 튜닝 후 launch 기본값 고정 — `vel_gain=0.4/vel_max=0.5/kf_accel_std=0.1` (8.2, 8.5)
- [ ] 착륙 게이트 저고도 핑퐁 여부 추가 관찰 후 `land_align_radius` 최종 확정
- [ ] 레거시 C++ 제어기(`offboard/src/precision_landing.cpp`) 정리 또는 동일 수정 반영
- [ ] `yolo_processor` 노드 동일 NumPy 2.x 이슈 — 필요 시 cv_bridge 제거 적용
- [ ] 실기체(SITL 외) 비행 검증 — 카메라 장착·조명·실제 마커 환경 확인

---

## 9. 움직이는 마커 추종 착륙 (2026-06-04, 구현 완료 · SITL 검증 대기)

**목표:** 마커 좌표가 외부에서 지속 송출되면 드론이 먼저 그 좌표로 **접근(APPROACH)**하고,
하방 카메라가 마커를 인식하는 순간 **비전 제어로 핸드오프**하여 (움직이는) 마커 위에 착륙한다.

### 9.1 새 노드: `moving_marker_node` (마커 이동 + 좌표 송출)

SITL에 자체 좌표 송출원이 없으므로, 마커를 움직이고 그 위치를 송출하는 노드를 신규 구현.

- Gazebo 모델(`aruco_marker_0`)을 궤적을 따라 이동 — **gz-transport13** 파이썬 바인딩으로
  `/world/iris_down_camera_runway/set_pose` 서비스 호출(텔레포트, 정적 지면 데칼이라 물리 불필요).
- 동일 틱에 마커 위치를 `geometry_msgs/PointStamped`(ENU)로 `/marker/position`에 발행.
- 궤적 패턴: `static` / `line`(동서 왕복) / `circle`. `speed`·`amplitude`·`center_e/n` 파라미터.
- **좌표계:** 드론 스폰 = Gazebo 월드 원점 = MAVROS local 원점 → **local ENU ≈ 월드 XY** (무변환).
- gz 바인딩이 없으면 **송출 전용**으로 강등(외부 publisher 대체/모델 미이동) — graceful degrade.

### 9.2 제어기에 APPROACH 단계 추가

상태기계 확장: `TAKEOFF → IDLE → **APPROACH** → ALIGN → DESCEND → DONE`

| 상태 | 동작 |
|------|------|
| `IDLE` | 마커가 보이면 ALIGN, 아니면 신선한 cue가 있으면 APPROACH |
| `APPROACH` | 송출 ENU 좌표(cue)로 속도 서보 비행(고도 유지). 카메라가 마커 잡으면 → ALIGN 핸드오프 |

- `/marker/position` 구독, `cue_timeout`(1.0s)로 죽은 publisher의 낡은 좌표 추종 방지.
- **비전 상실 시 폴백:** ALIGN에서 마커를 coast 한계까지 놓치면 IDLE 대신 **APPROACH로 복귀**
  (cue가 살아 있으면 좌표 기반으로 계속 추적) → 끊김에 강건.
- `use_cue=false`면 기존 순수 비전 동작으로 복귀.

### 9.3 속도 피드포워드 — 움직이는 표적 정상상태 지연 제거 (핵심)

순수 비례제어 `v=kp·e`만으로 등속 이동 표적을 쫓으면 정상상태 지연 `v_target/kp`(예: 0.3/0.4 ≈ 0.75 m)가
남아 착륙 게이트(0.20 m)를 통과하지 못한다. 표적 위에 멈추지 못하고 계속 끌려다님.

- **해결:** 서보를 `v = v_target_ff + kp·e`로 변경(`_servo_to`에 피드포워드 인자 추가).
- **APPROACH:** cue를 후방차분(`_cue_cb`)하여 cue 속도를 피드포워드.
- **ALIGN/DESCEND:** **칼만필터가 추정하는 마커 속도**(`kf_x[2:4]`)를 그대로 피드포워드 →
  비전 단계에서도 무지연 추종. 정지 마커는 `v_marker≈0`이라 기존 ~2 cm 거동 보존.
- (이동 마커 추적 시 `kf_accel_std`를 0.1 → ~0.5로 올려 속도 추정 반응성 확보 권장.)

### 9.4 변경 파일

| 파일 | 변경 |
|------|------|
| `precision_landing/moving_marker_node.py` | **신규** — gz 마커 이동 + ENU 좌표 송출 |
| `precision_landing/precision_landing_node.py` | APPROACH 단계, cue 구독/타임아웃, 속도 피드포워드 |
| `precision_landing/launch/precision_landing.launch.py` | `moving_marker` 노드 + `use_cue`/`marker_*` 인자 |
| `precision_landing/setup.py` | `moving_marker_node` 엔트리포인트 |

### 9.5 검증 상태 및 남은 작업

- [x] 빌드·노드 등록·import 확인, `/marker/position` 송출 동작 확인(시뮬 없이 publish-only)
- [ ] **풀 SITL 비행 검증** — set_pose로 정적 모델 텔레포트 동작, APPROACH→비전 핸드오프, 이동 마커 착륙
- [ ] 이동 마커용 `kf_accel_std` 재튜닝 + 착륙 게이트 통과 여부 확인
- [ ] (선택) circle 패턴·고속 마커에서 추종 한계 측정
